{
  "name": "npm-supply-scan",
  "version": "0.2.0",
  "description": "Lightning-fast npm supply-chain attack scanner with pluggable threat profiles.",
  "type": "module",
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build src/cli.ts --compile --outfile bin/npm-supply-scan",
    "test": "bun test",
    "lint": "bunx eslint ."
  },
  "bin": {
    "npm-supply-scan": "bin/npm-supply-scan"
  },
  "devDependencies": {
    "@types/bun": "^1.2.22",
    "@types/node": "24.5.1",
    "typescript": "5.9.2",
    "eslint": "9.35.0"
  },
  "dependencies": {
  }
}
